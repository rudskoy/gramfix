import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Global shortcut to toggle Clipsa window visibility
    static let toggleClipsa = Self("toggleClipsa", default: .init(.backslash, modifiers: [.command, .shift]))
}

